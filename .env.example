# This project currently does not require any environment variables or API keys.
# This file is kept as a placeholder in case a local file path or credential
# needs to be added later (for example, a custom data directory).
#
# DATA_DIR=./data
